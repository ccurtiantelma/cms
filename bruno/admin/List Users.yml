info:
  name: List Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/app/admin/users?p=1&i=20"
  params:
    - name: p
      value: "1"
      type: query
    - name: i
      value: "20"
      type: query
    - name: q
      value: ""
      type: query
      disabled: true
    - name: o
      value: name
      type: query
      disabled: true
    - name: d
      value: asc
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
