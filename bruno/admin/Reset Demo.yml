info:
  name: Reset Demo
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/app/admin/system/reset-demo"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
