info:
  name: Get Theme
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/app/settings/theme"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
