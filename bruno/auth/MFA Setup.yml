info:
  name: MFA Setup
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseUrl}}/auth/mfa-setup"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
