info:
  name: MFA Disable
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/auth/mfa-disable"
  body:
    type: json
    data: |-
      {
        "code": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
