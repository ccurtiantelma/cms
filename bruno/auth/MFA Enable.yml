info:
  name: MFA Enable
  type: http
  seq: 13

http:
  method: POST
  url: "{{baseUrl}}/auth/mfa-enable"
  body:
    type: json
    data: |-
      {
        "code": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
