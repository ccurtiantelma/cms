info:
  name: Get Sessions
  type: http
  seq: 17

http:
  method: GET
  url: "{{baseUrl}}/auth/sessions"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
